# 2026-05-03

- 浏览 Magic Writer 项目：确认这是 AI 驱动的桌面端小说创作工具，采用 pnpm monorepo，主应用为 apps/desktop，核心包为 shared、llm-gateway、agent-core、rag；当前为 M0 原型骨架，已跑通 Electron + React + Monaco + IPC + Mock/兼容 LLM 流式续写链路。
- 生成完整的产品需求文档 PRD，输出到 docs/PRD.md。包含产品概览、用户旅程、功能模块（项目管理/编辑器/AI Agent/世界观/RAG）详述及状态标注、竞品分析、路线图（M0~M3）、验收标准、关键指标、风险评估。
- 生成设计规范文档 DESIGN-SYSTEM.md，输出到 docs/DESIGN-SYSTEM.md。涵盖设计哲学、色彩系统（深色主题 token）、排版体系（编辑器/UI 字体栈）、布局规范（三栏 + 写作模式）、组件设计（按钮/输入框/对话气泡/命令面板等）、动效规范、Agent 交互模式、世界观库 UI、无障碍设计、设计交付清单（M1/M2）。
- 生成前后端架构文档（TypeScript 全栈）：
  - docs/ARCHITECTURE-FRONTEND.md：Renderer 层架构（React+Zustand+Monaco）、组件分层、Store 设计、IPC 通信模型、样式系统、性能策略、测试金字塔。
  - docs/ARCHITECTURE-BACKEND.md：Main Process 架构（Node.js）、Storage 模块（JSON→SQLite 演进）、完整 SQL Schema、Agent 编排流程、LLM Gateway（Provider 适配器+降级链+SSE 解析）、RAG 引擎（Chunker/Embedder/VectorStore）、Worker 池、安全架构、IPC 完整契约。
- M1 核心开发实现（全部构建通过）：
  - 存储层从 JSON 重构为 better-sqlite3 + FTS5（database.ts 迁移系统 + storage/index.ts 全面重写）
  - 侧栏 ⌘B/⌘J 独立切换修复（正确订阅 sidebarVisible/agentPanelVisible）
  - Monaco 选区接入 Agent（__mwGetSelection 全局暴露 + AgentPanel 自动读取）
  - 自动保存（2 秒 debounce + 每日码字量统计）
  - 上下文组装完善（人物卡注入 + 章节大纲注入 + RAG 检索注入）
  - RAG 最小闭环（rag-engine.ts：保存时自动 chunk+embed，续写时检索 top-5）
  - 命令面板 ⌘K（CommandPalette.tsx：搜索过滤 + 键盘导航 + 执行）
  - 每日字数目标 + 进度条 + 达成庆祝动画（DailyGoal.tsx）
  - 世界观库 UI（WorldPanel.tsx：人物卡列表/新建/编辑/删除/锁定字段）
  - Preload 扩展（world/settings/search API）
  - IPC 扩展（世界观 CRUD + 全文搜索 + 设置 KV）
  - Seed 数据包含示例人物卡（萧远）
- M2 功能开发实现（全部构建通过）：
  - 大纲 Agent 消费 UI（OutlinePanel.tsx：生成/解析 JSON 大纲，三幕→卷→章可折叠树）
  - 设置面板（SettingsPanel.tsx：LLM Provider/Model/Key、字体、字号、每日目标字数配置，通过 settings IPC 持久化）
  - ReviewAgent 审校报告 UI（ReviewReport.tsx：解析 JSON 冲突列表，分级展示 高/中/低 优，支持跳转到对应章节）
  - 写作模式增强（编辑区居中 max-w-680px、加大 padding、更大字号行高，打字机体验）
  - Agent 中断功能（agent store 添加 stop 方法 + currentRequestId 追踪）
  - 伏笔追踪面板（ForeshadowingPanel.tsx：埋伏笔 / 标记回收 / 废弃 / 分组展示）
  - Agent 面板专属视图（outline→OutlinePanel，review→ReviewReport，其他→通用对话）
  - 侧栏三 Tab 架构（📖章节 / 🌍世界观 / 🎯伏笔）
  - 解决 better-sqlite3 NODE_MODULE_VERSION 不匹配问题（electron-rebuild）
- M2 剩余功能实现（全部构建通过）：
  - 人物关系图（RelationGraph.tsx：基于 @xyflow/react，环形自动布局，自定义 CharacterNode，关系边标注）
  - 风格学习（style-learner.ts：从项目已有章节提取代表性段落，分类为叙述/对话/动作/描写，格式化为 few-shot 注入 Agent prompt）
  - Worker 进程池（worker/pool.ts：最多 2 Worker，任务队列，支持 embed-batch/count-words/analyze-consistency）
  - 多模型动态切换（llm/index.ts 重写：优先读 SQLite settings → 环境变量 → 默认 mock，每次调用刷新配置）
  - WorldPanel 增加关系图视图切换（list/graph）
  - 应用退出时关闭 Worker 池 + 数据库
- 全局 Emoji → SVG Icon 替换：
  - 创建 Icons.tsx SVG Icon 组件库（20+ 图标：Writer/Polish/Review/Outline/Book/Globe/Target/Network/Lock/Unlock/Settings/Plus/Celebrate/Wand/Lightbulb/ArrowLeft/Check/X/Sparkles）
  - 替换 9 个组件文件中的所有 emoji 为 SVG Icon：AgentPanel、Sidebar、WorldPanel、ForeshadowingPanel、DailyGoal、OutlinePanel、ReviewReport、SettingsPanel、RelationGraph
  - 确认 Renderer 层零 emoji 残留
- UI 架构重大重构：
  - 新增 NavBar.tsx（全局左侧导航栏：Logo + 菜单项 + 当前作品快捷入口 + 设置）
  - 新增 LibraryView.tsx（作品库主页：卡片网格展示全部作品，新建/删除作品，点击进入写作工作台）
  - App.tsx 拆分为两个视图：library（NavBar+LibraryView）和 workspace（NavBar+Sidebar+Editor+AgentPanel）
  - Sidebar 简化为纯作品内部面板（章节/世界观/伏笔 tabs，去掉作品切换逻辑）
  - 新增 ContextMenu.tsx 通用右键菜单组件
  - 右键菜单交互：空白区→新建卷、卷→新建章节/删除卷、章节→新建章节/删除章节
  - 后端新增 deleteProject/deleteVolume/deleteChapter + volume:create/delete + chapter:delete + project:delete IPC
  - 色板从蓝紫偏色调整为中性灰（surface-900:#111113 → surface-600:#303033），拉大层级明度差
  - 输入框添加 border，滚动条美化
